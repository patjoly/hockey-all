To-do list for my hockey-all repository

Next Steps:
	xG_modelling.R:


	xG_preparation.R:
		. translate fun.pbp_model_prep() to data.table syntax
		. continue coding fun.pbp_prep() and fun.model_prep() for the other prep_types
		. move all col creation that can be done in fun.pbp_expand() there e.g. score_state, is_home


Pad -- just a temporary section -- don't commit this
	Remember can use a mask, such as to use a subset of the data
		mask <- pbp_part$event_zone == 'Neu'
		df <- pbp_part[ mask, ]
		table( df$coords_x, df$event_zone )

		Just make sure the mask doesn't have NA values, must be fully TRUE/FALSE


